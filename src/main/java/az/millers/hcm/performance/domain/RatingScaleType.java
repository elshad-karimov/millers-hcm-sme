package az.millers.hcm.performance.domain;

/** HCM_12 M388 — kind of rating scale (PRD §5.3). */
public enum RatingScaleType {
    NUMERIC_1_5,
    NUMERIC_1_10,
    PERCENTAGE,
    DESCRIPTIVE,
    LETTER,
    PASS_FAIL
}
