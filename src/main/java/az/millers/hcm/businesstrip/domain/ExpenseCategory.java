package az.millers.hcm.businesstrip.domain;

/** Expense line-item categories (M104). Mirrors the V71 CHECK. */
public enum ExpenseCategory {
    ACCOMMODATION,
    MEALS,
    TRANSPORT,
    FLIGHT,
    VISA_FEES,
    COMMUNICATION,
    REGISTRATION,
    OTHER
}
