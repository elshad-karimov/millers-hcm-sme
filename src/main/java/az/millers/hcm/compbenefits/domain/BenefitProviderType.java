package az.millers.hcm.compbenefits.domain;

/** HCM_11 M374 — kind of benefit provider / vendor. */
public enum BenefitProviderType {
    INSURER,
    PENSION_FUND,
    CLINIC,
    VENDOR,
    BANK,
    OTHER
}
