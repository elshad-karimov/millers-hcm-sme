# HCM_34–49 Bundle — Analysis & Milestone Plan

Analyzer + gap-checker both PASS (2026-07-05). Milestones **M481–M494**, migrations **V295–V308** (indicative — allocate sequentially). payroll_impact: false — nothing crosses rule 3: rewards bridge via existing addBonus(ONE_TIME); scheduling feeds the existing roster→attendance→payroll chain; costing/timesheets allocate only; mobility = tracking; contractors excluded from statutory payroll.

## Coverage (platform ≈75% of the 16 modules already)

| Module | % | Plan |
|---|---|---|
| 34 Engagement | 92 | 0 milestones |
| 41 Case Mgmt | 85 / 43 Probation | 88 / 45 Disciplinary | 88 / 46 Letters | 90 | 0 milestones each |
| 36 Scheduling | 70 → M482 | 42 Policy | 80 → M490 | 44 Promotion | 80 → M491 | 48 Security | 82 → M494 |
| 35 Rewards | 55 → M481+M483 | 47 Notifications | 75 → M493 | 49 Integration | 50 → M492 |
| 37 Costing | 45 → M485+M486 | 38 Project TS | 35 → M484 | 40 Contingent | 25 → M488+M489 | 39 Mobility | 0 → M487 |

## Milestones

### Phase A — Rewards & Scheduling (M481–M483)
- **M481 (V295, M)** reward catalog + points wallet + budgets: engagement.reward_catalog (code, name, points INT, monetary_value NUMERIC NULL [1 point = 1 AZN default], category, taxable BOOL, active) + reward_wallet (employee_id UNIQUE per tenant, balance INT, updated_at) + reward_budget (year, org_unit_id NULL, total NUMERIC, used NUMERIC). RewardService: grantPoints (HR/manager within budget — budget check + used increment), redeem (employee: wallet balance ≥ catalog points → decrement + create redemption record [add engagement.reward_redemption table: employee, catalog item, points, status REQUESTED/FULFILLED, fulfilled_by/at]; monetary items: on FULFILL bridge via existing PayrollRunService.addBonus ONE_TIME pattern like M295 — non-fatal, audited). SPA: RewardCatalogPage (/engagement/rewards: catalog admin + redemption queue for HR) + "My points" wallet card + redeem action on MyWorkspace or the recognition page.
- **M482 (V296, M)** open shifts + swap requests: attendance.open_shift (shift_id FK, date, org_unit_id, slots INT, filled INT, status OPEN/FILLED/CLOSED) + claim endpoint (employee claims → roster entry created, filled++) + attendance.shift_swap_request (roster_entry_id, from_employee, to_employee, status PENDING/APPROVED/REJECTED, approved_by) — manager-of-from-employee or HR approves (self-approve blocked), on approve swap the roster entry's employee + audit both. SPA: Open shifts + Swap requests tabs on the existing roster page + employee-facing claim/swap actions (MyWorkspace or roster views — match existing patterns).
- **M483 (V297 or none, S)** service anniversary auto-recognition: daily job finds employees whose hire anniversary (whole years ≥1) is today → creates EmployeeRecognition (system sender or HR user, value tag — add SERVICE_ANNIVERSARY to the tag enum/values, message "X years of service", PUBLIC) idempotent per employee+year (dedupe check). Config toggle via tenant_setting engagement_service_awards (default true).

### Phase B — Project Timesheets & Costing (M484–M486)
- **M484 (V298, M)** project dimension on timesheets: first CHECK what projects exist — grep for a project entity; if none, create timesheet.project master (code, name, active, billing_rate NUMERIC NULL) in this migration. ALTER timesheet_day (verify actual table via \d) ADD project_id UUID NULL, task_code VARCHAR(60) NULL, billable BOOL DEFAULT false. Service validation: project must be active when set. SPA: project/task/billable columns or per-day editor on the existing timesheet grid (keep it simple — a per-day drawer/edit if inline is complex) + projects admin mini-page.
- **M485 (V299, M)** labor rates + cost allocation: payroll.labor_rate (grade_id NULL, position_id NULL, hourly_rate NUMERIC, effective_from/to; lookup precedence position > grade, latest effective ≤ date) + payroll.labor_cost_allocation (timesheet_day_id, employee_id, work_date, cost_center_id NULL, project_id NULL, hours NUMERIC, rate NUMERIC, amount NUMERIC). CostingCalculator: on timesheet APPROVAL (find the approval hook) compute allocations per day (rate lookup; cost center from employee's CostCenterAllocation splits if present else 100% employee org-unit; project from the day's project_id) — idempotent per timesheet (regenerate = delete+recreate its rows), NON-FATAL, never touches payroll tables. Rates CRUD HR_ADMIN/Finance.
- **M486 (no migration, S)** labor cost reports: LaborCostReportService — by project, by department/cost center, by month; variance vs budget optional-skip. GET /api/reports/labor-cost?year&month&groupBy=. Finance/HR roles. LaborCostReportPage.

### Phase C — Mobility & Contingent (M487–M489)
- **M487 (V301, M)** international assignments (TRACKING ONLY): schema mobility. international_assignment (employee_id, assignment_no IA-seq, host_country VARCHAR(80), host_city, host_entity VARCHAR(200) NULL, purpose, start_date, end_date NULL, status PLANNED/ACTIVE/COMPLETED/CANCELLED, visa_type VARCHAR(60) NULL, visa_expiry DATE NULL, housing_allowance/cola_amount/hardship_amount NUMERIC NULL [manual-entry tracking fields], notes). Documents via AttachmentUploader (owner_entity=internationalassignment). Visa expiry into the expiring-docs pattern (endpoint GET /api/mobility/assignments/expiring-visas?days=90). Status transitions audited. HR_ADMIN/HR_SPECIALIST; the employee may VIEW their own assignment. SPA InternationalAssignmentsPage /mobility/assignments (+ attachments in drawer).
- **M488 (V302, M)** contractor engagements: contractors = Employee with employment_type CONTRACTOR (verify enum value exists) + contingent.contractor_engagement (employee_id UNIQUE per tenant, vendor/agency_id UUID NULL [FK recruitment agency if schema fits — verify], contract_start, contract_end NULL, rate NUMERIC NULL, rate_unit HOURLY/DAILY/MONTHLY, po_number VARCHAR(60) NULL, tenure_alert_days INT DEFAULT 30, status ACTIVE/ENDED/CONVERTED, notes). RATE + PO = Finance/HR-confidential (mask for others incl. managers). Contract-end alerts (expiry pattern). ContractorsPage /contingent/contractors (list joins employee where employment_type=CONTRACTOR, engagement editor).
- **M489 (V303 or none, S)** contractor→FTE conversion: ConversionService.convert(engagementId, newEmploymentType, effectiveDate): guard status ACTIVE → flip employee.employment_type (via existing employee update path — find it, keep audit/history), engagement → CONVERTED + conversion_date, optionally start the standard onboarding checklist via ChecklistService.startMatched (non-fatal). HR_ADMIN. Endpoint + Convert button/modal on ContractorsPage. Simple direct action + audit (workflow optional — skip unless trivial).

### Phase D — Policy & Movement (M490–M491)
- **M490 (V304, M)** policy re-acknowledgement campaigns: policy.acknowledgement_campaign (policy_id FK, policy_version, audience ALL/DEPARTMENT (+audience_ref UUID NULL), due_date DEFAULT +14d, status DRAFT/ACTIVE/CLOSED, launched_at). launch(): notify audience employees (NotificationService, batched); progress(): acked (PolicyAcknowledgement rows for that version, in-audience) vs total + per-department; close(). PolicyCampaignsPage (or a Campaigns tab on the existing policy admin page) with progress bars.
- **M491 (no migration or V305 if a column needed, S)** movement execution wiring: MovementExecutionService.execute(requestId): require status APPROVED → apply the change via existing services (TRANSFER: employee org_unit/position update path or PositionTransferService — grep and reuse; PROMOTION: position/grade update; salary changes are NOT auto-applied — surface a note that comp changes go through the existing salary-change request flow) → request status EXECUTED (add enum value + executed_at/by columns if missing — check V254 table) + audit. Execute button (HR_ADMIN) on MovementRequestsPage for APPROVED rows.

### Phase E — Integration & Notifications (M492–M493)
- **M492 (V306, M)** integration registry: schema integration. integration_config (code UNIQUE per tenant, name, direction INBOUND/OUTBOUND, type WEBHOOK/FILE/API, endpoint_url VARCHAR(500) NULL [encrypt via EncryptedStringConverter like webhook_url — copy that pattern], credentials_ref VARCHAR(200) NULL [reference name only, never secrets], enabled BOOL, last_run_at, last_status VARCHAR(20), notes) + integration_log (config FK, run_at, status SUCCESS/FAILED, records_processed INT, error_message VARCHAR(1000)) + 90-day purge job. Registry CRUD SYSTEM_ADMIN-only + a recordRun() service hook others can call later (seam). IntegrationsPage /admin/integrations (config table + log drawer). NO live connectors.
- **M493 (V307, M)** notification templates + delivery log: notification.notification_template (code UNIQUE per tenant, channel EMAIL/IN_APP, subject_template, body_template, active) + notification.delivery_log (channel, recipient, subject, status SENT/FAILED, error VARCHAR(500), sent_at, source_module). Wire delivery_log writes into NotificationService + EmailService send paths (non-fatal). Template admin (HR_ADMIN) + DeliveryLogsPage (HR_ADMIN, filters). Existing hardcoded notification texts stay — templates used where a template code matches (seam; document).

### Phase F — Security visibility (M494)
- **M494 (no migration, S)** permission matrix view: PermissionMatrixService returns a static-built matrix of role → capability areas (curate from SecurityRoles constants + module list — hand-maintained structure in code is FINE, document it). GET /api/security/permission-matrix (SYSTEM_ADMIN/AUDITOR/HR_ADMIN read-only) + PermissionMatrixPage /admin/permission-matrix (roles × capabilities grid). Plus surface the existing session-ish info ONLY if trivial (skip otherwise).

## Roles
Contractor rate/PO + labor rates/costs + reward budgets = HR/Finance confidential (mask elsewhere). Mobility visa data HR + own employee. Integration configs SYSTEM_ADMIN. Reward redemption fulfil = HR. Matrix read-only SYSTEM_ADMIN/AUDITOR/HR_ADMIN.

## Adopted defaults
1 point = 1 AZN (configurable); wallet no expiry; monetary redemption → ONE_TIME bonus bridge; shift swap = single manager approval, self-approve blocked, OT stays with existing engine; coverage minimums deferred (alert-only concept, not in plan); labor rate lookup position>grade latest-effective; billable rate chain project→labor rate; mobility = tracking + manual allowance fields + per-diem tables reusable for trips; contractor tenure alert 30d; contractors excluded from statutory payroll (existing behavior — verify not regressed); policy campaign due +14d; integration log retention 90d; SMS/push deferred; matrix = curated static structure; case/engagement/letters/probation/disciplinary modules: NO new milestones (existing coverage cited in table).
