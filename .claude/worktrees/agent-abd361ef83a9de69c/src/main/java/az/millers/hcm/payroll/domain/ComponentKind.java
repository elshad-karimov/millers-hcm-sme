package az.millers.hcm.payroll.domain;

/**
 * M349 — Salary component kind: EARNING (adds to compensation) or DEDUCTION (reduces net pay).
 */
public enum ComponentKind {
    EARNING,
    DEDUCTION
}
