package az.millers.hcm.lifecycle.domain;

/** Contract change types (PRD 8.12.1). */
public enum ChangeType {
    SALARY,
    POSITION,
    DEPARTMENT,
    MANAGER,
    GRADE,
    LOCATION,
    JOB_TITLE,
    EMPLOYMENT_TYPE,
    COST_CENTRE
}
